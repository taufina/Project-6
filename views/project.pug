extends layout.pug

block content 
  nav.grid-container.portfolio-breadcrumb
    //- link back to the home page
    a(href='/') ← Back  
  article.grid-container.portfolio-intro.portfolio-project
    .grid-x
      .cell.small-6.medium-9.large-9

        //- project title.
        h1 #{project.project_name}

        //- project description.
        p.lead.text-light
          | #{project.description}

      .cell.portfolio-meta.small-6.medium-2.large-3
        h6 Technologies

        //- technologies I used in each project.
        ul
          each technology in project.technologies
            li= technology
          

        //- links to the repo and the live version of each project.
        a.button.expanded(target="_blank" href=`${project.live_link}`) Live Demo
        a.button.expanded(target="_blank" href=`${project.github_link}`) GitHub Repo
        br
    

      //- Project images.

      each image in project.image_urls
        img.image(src=image)
   
      //- img.image(src=`${project.image_urls[1]}`)
      //- img.image(src=`${project.image_urls[2]}`)